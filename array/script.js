let fruits = ['Apple', 'Banana', 'Mango'];
//////////// [  0        1         2     ]
// console.log(fruits.length)

// let first = fruits[0]

// let last = fruits[fruits.length - 1]

// let newLength = fruits.push('Orange')

// let last = fruits.pop() // remove Orange (from the end)

// let first = fruits.shift() // remove Apple from the front

// let newLength = fruits.unshift('Strawberry') // add to the front

// let shallowCopySpread = [...fruits];
// let shallowCopySlice = fruits.slice();
// let shallowCopyFrom = Array.from(fruits);

// These all create a shallow copy;
// top-level elements containing primitive values are copied,
// but if the array contains nested objects or arrays,
// those will reference elements in the original array'

// let deepCopy = JSON.parse(JSON.stringify(fruits));
